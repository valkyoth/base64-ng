[package]
name = "base64-ng-perf"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[features]
default = ["simd"]
simd = ["base64-ng/simd"]

[dependencies]
base64 = "0.22.1"
base64-ng = { path = "..", version = "1.2.0", default-features = false, features = ["alloc", "std"] }

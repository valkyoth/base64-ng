[release]
version = "1.0.10"
policy = "core-first-companion"

[crates."base64-ng"]
previous_version = "1.0.9"
version = "1.0.10"
change = "code"
publish = true
reason = "Splits oversized internal source modules and adds the production-source 500-line budget guard."

[crates."base64-ng-sanitization"]
previous_version = "1.0.9"
version = "1.0.9"
change = "unchanged"
publish = false
reason = "No code changes for the 1.0.10 core maintenance release."

[crates."base64-ng-derive"]
previous_version = "1.0.9"
version = "1.0.9"
change = "unchanged"
publish = false
reason = "No code changes for the 1.0.10 core maintenance release."

[crates."base64-ng-serde"]
previous_version = "1.0.9"
version = "1.0.10"
change = "code"
publish = true
reason = "Adds drop-time cleanup and explicit comparison behavior for serde wrapper types."

[crates."base64-ng-bytes"]
previous_version = "1.0.9"
version = "1.0.9"
change = "unchanged"
publish = false
reason = "No code changes for the 1.0.10 core maintenance release."

[crates."base64-ng-tokio"]
previous_version = "1.0.9"
version = "1.0.9"
change = "unchanged"
publish = false
reason = "No code changes for the 1.0.10 core maintenance release."

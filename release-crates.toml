[release]
version = "2.0.3"
policy = "synced-family"

[crates."base64-ng"]
previous_version = "2.0.2"
version = "2.0.3"
change = "code"
publish = true
reason = "Publishes the unchanged 2.0 runtime with sanitization 2.0.4, current tooling and GitHub Actions, and Rust 1.98.1 release checks."

[crates."base64-ng-sanitization"]
previous_version = "2.0.2"
version = "2.0.3"
change = "dependency"
publish = true
reason = "Synchronizes the companion with base64-ng 2.0.3 and sanitization 2.0.4."

[crates."base64-ng-derive"]
previous_version = "2.0.2"
version = "2.0.3"
change = "dependency"
publish = true
reason = "Synchronizes the companion dependency with base64-ng 2.0.3."

[crates."base64-ng-imap"]
previous_version = "2.0.2"
version = "2.0.3"
change = "dependency"
publish = true
reason = "Synchronizes the companion dependency with base64-ng 2.0.3."

[crates."base64-ng-mime"]
previous_version = "2.0.2"
version = "2.0.3"
change = "dependency"
publish = true
reason = "Synchronizes the companion dependency with base64-ng 2.0.3."

[crates."base64-ng-multibase"]
previous_version = "2.0.2"
version = "2.0.3"
change = "dependency"
publish = true
reason = "Synchronizes the companion dependency with base64-ng 2.0.3."

[crates."base64-ng-password"]
previous_version = "2.0.2"
version = "2.0.3"
change = "dependency"
publish = true
reason = "Synchronizes the companion dependency with base64-ng 2.0.3."

[crates."base64-ng-openpgp"]
previous_version = "2.0.2"
version = "2.0.3"
change = "dependency"
publish = true
reason = "Synchronizes the companion dependency with base64-ng 2.0.3."

[crates."base64-ng-pem"]
previous_version = "2.0.2"
version = "2.0.3"
change = "dependency"
publish = true
reason = "Synchronizes the companion dependency with base64-ng 2.0.3."

[crates."base64-ng-serde"]
previous_version = "2.0.2"
version = "2.0.3"
change = "dependency"
publish = true
reason = "Synchronizes the companion dependency with base64-ng 2.0.3."

[crates."base64-ng-bytes"]
previous_version = "2.0.2"
version = "2.0.3"
change = "dependency"
publish = true
reason = "Synchronizes the companion dependency with base64-ng 2.0.3."

[crates."base64-ng-subtle"]
previous_version = "2.0.2"
version = "2.0.3"
change = "dependency"
publish = true
reason = "Synchronizes the companion dependency with base64-ng 2.0.3."

[crates."base64-ng-tokio"]
previous_version = "2.0.2"
version = "2.0.3"
change = "dependency"
publish = true
reason = "Synchronizes the companion dependency with base64-ng 2.0.3."

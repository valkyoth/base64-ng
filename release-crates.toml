[release]
version = "1.3.7"
policy = "synced-family"

[crates."base64-ng"]
previous_version = "1.3.6"
version = "1.3.7"
change = "dependency"
publish = true
reason = "Publishes the 1.3.7 maintenance patch on Rust 1.97.0 with synchronized crate versions and audited tooling pins while deferring the stronger RISC-V RVV proof and admission review to 1.3.8."

[crates."base64-ng-sanitization"]
previous_version = "1.3.6"
version = "1.3.7"
change = "dependency"
publish = true
reason = "Keeps the sanitization companion synchronized and updates its exact sanitization dependency to 1.2.4."

[crates."base64-ng-derive"]
previous_version = "1.3.6"
version = "1.3.7"
change = "dependency"
publish = true
reason = "Keeps the derive companion version synchronized with the 1.3.7 crate family."

[crates."base64-ng-serde"]
previous_version = "1.3.6"
version = "1.3.7"
change = "dependency"
publish = true
reason = "Keeps the serde companion version synchronized with the 1.3.7 crate family."

[crates."base64-ng-bytes"]
previous_version = "1.3.6"
version = "1.3.7"
change = "dependency"
publish = true
reason = "Keeps the bytes companion synchronized and updates bytes to 1.12.1."

[crates."base64-ng-subtle"]
previous_version = "1.3.6"
version = "1.3.7"
change = "dependency"
publish = true
reason = "Keeps the subtle companion version synchronized with the 1.3.7 crate family."

[crates."base64-ng-tokio"]
previous_version = "1.3.6"
version = "1.3.7"
change = "dependency"
publish = true
reason = "Keeps the Tokio companion version synchronized with the 1.3.7 crate family."

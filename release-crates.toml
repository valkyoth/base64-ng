[release]
version = "1.3.9"
policy = "synced-family"

[crates."base64-ng"]
previous_version = "1.3.8"
version = "1.3.9"
change = "dependency"
publish = true
reason = "Keeps the core package synchronized with the 1.3.9 crate family while the sanitization companion migrates to sanitization 2.0.3; the stronger RISC-V RVV review moves to 1.3.10."

[crates."base64-ng-sanitization"]
previous_version = "1.3.8"
version = "1.3.9"
change = "code"
publish = true
reason = "Migrates the sanitization companion to exact-pinned sanitization 2.0.3, allocation-time fail-closed fixed and bounded dynamic locked decode, detailed protection and integrity errors, and the hardened native feature profile."

[crates."base64-ng-derive"]
previous_version = "1.3.8"
version = "1.3.9"
change = "dependency"
publish = true
reason = "Keeps the derive companion version synchronized with the 1.3.9 crate family."

[crates."base64-ng-serde"]
previous_version = "1.3.8"
version = "1.3.9"
change = "dependency"
publish = true
reason = "Keeps the serde companion version synchronized with the 1.3.9 crate family."

[crates."base64-ng-bytes"]
previous_version = "1.3.8"
version = "1.3.9"
change = "dependency"
publish = true
reason = "Keeps the bytes companion synchronized with the 1.3.9 crate family."

[crates."base64-ng-subtle"]
previous_version = "1.3.8"
version = "1.3.9"
change = "dependency"
publish = true
reason = "Keeps the subtle companion version synchronized with the 1.3.9 crate family."

[crates."base64-ng-tokio"]
previous_version = "1.3.8"
version = "1.3.9"
change = "dependency"
publish = true
reason = "Keeps the Tokio companion version synchronized with the 1.3.9 crate family."

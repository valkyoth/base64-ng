[release]
version = "1.3.6"
policy = "synced-family"

[crates."base64-ng"]
previous_version = "1.3.5"
version = "1.3.6"
change = "dependency"
publish = true
reason = "Publishes the 1.3.6 family patch with synchronized crate versions, refreshed companion crate README headers, staged CT decode docs, and a high-assurance compile cfg guard."

[crates."base64-ng-sanitization"]
previous_version = "1.3.5"
version = "1.3.6"
change = "dependency"
publish = true
reason = "Keeps the sanitization companion version synchronized and publishes the refreshed README header."

[crates."base64-ng-derive"]
previous_version = "1.3.5"
version = "1.3.6"
change = "dependency"
publish = true
reason = "Keeps the derive companion version synchronized and publishes the refreshed README header."

[crates."base64-ng-serde"]
previous_version = "1.3.5"
version = "1.3.6"
change = "dependency"
publish = true
reason = "Keeps the serde companion version synchronized and publishes the refreshed README header."

[crates."base64-ng-bytes"]
previous_version = "1.3.5"
version = "1.3.6"
change = "dependency"
publish = true
reason = "Keeps the bytes companion version synchronized and publishes the refreshed README header."

[crates."base64-ng-subtle"]
previous_version = "1.3.5"
version = "1.3.6"
change = "dependency"
publish = true
reason = "Keeps the subtle companion version synchronized and publishes the refreshed README header."

[crates."base64-ng-tokio"]
previous_version = "1.3.5"
version = "1.3.6"
change = "dependency"
publish = true
reason = "Keeps the Tokio companion version synchronized and publishes the refreshed README header."

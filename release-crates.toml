[release]
version = "1.3.3"
policy = "synced-family"

[crates."base64-ng"]
previous_version = "1.3.2"
version = "1.3.3"
change = "code"
publish = true
reason = "Publishes the 1.3.3 family patch with narrow wasm simd128 runtime dispatch, runtime smoke evidence, codegen evidence, wrapped-profile helper ergonomics, wrapped/legacy decode staging, and stack-staged in-place encode through admitted backends."

[crates."base64-ng-sanitization"]
previous_version = "1.3.2"
version = "1.3.3"
change = "dependency"
publish = true
reason = "Keeps the sanitization companion version synchronized with the 1.3.3 family release."

[crates."base64-ng-derive"]
previous_version = "1.3.2"
version = "1.3.3"
change = "dependency"
publish = true
reason = "Keeps the derive companion version synchronized with the 1.3.3 family release."

[crates."base64-ng-serde"]
previous_version = "1.3.2"
version = "1.3.3"
change = "dependency"
publish = true
reason = "Keeps the serde companion version synchronized with the 1.3.3 family release."

[crates."base64-ng-bytes"]
previous_version = "1.3.2"
version = "1.3.3"
change = "dependency"
publish = true
reason = "Keeps the bytes companion version synchronized with the 1.3.3 family release."

[crates."base64-ng-subtle"]
previous_version = "1.3.2"
version = "1.3.3"
change = "dependency"
publish = true
reason = "Keeps the subtle companion version synchronized with the 1.3.3 family release."

[crates."base64-ng-tokio"]
previous_version = "1.3.2"
version = "1.3.3"
change = "dependency"
publish = true
reason = "Keeps the Tokio companion version synchronized with the 1.3.3 family release."

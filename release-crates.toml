[release]
version = "2.0.0"
policy = "development-blocked"

[crates."base64-ng"]
previous_version = "1.3.9"
version = "2.0.0"
change = "code"
publish = false
reason = "Develops the 2.0 core API under numbered, pentest-covered checkpoints; publication remains blocked until final acceptance."

[crates."base64-ng-sanitization"]
previous_version = "1.3.9"
version = "2.0.0"
change = "code"
publish = false
reason = "Develops the synchronized 2.0 sanitization integration; publication remains blocked until final acceptance."

[crates."base64-ng-derive"]
previous_version = "1.3.9"
version = "2.0.0"
change = "code"
publish = false
reason = "Tracks the synchronized 2.0 derive integration; publication remains blocked until final acceptance."

[crates."base64-ng-imap"]
previous_version = "0.0.0"
version = "2.0.0"
change = "code"
publish = false
reason = "Develops the bounded RFC 3501 Section 5.1.3 modified-Base64 payload companion; publication remains blocked until final acceptance."

[crates."base64-ng-mime"]
previous_version = "0.0.0"
version = "2.0.0"
change = "code"
publish = false
reason = "Develops the bounded RFC 2045 Section 6.8 content-transfer body companion; publication remains blocked until final acceptance."

[crates."base64-ng-multibase"]
previous_version = "0.0.0"
version = "2.0.0"
change = "code"
publish = false
reason = "Develops the bounded Base64-family multibase companion against a pinned registry; publication remains blocked until final acceptance."

[crates."base64-ng-password"]
previous_version = "0.0.0"
version = "2.0.0"
change = "code"
publish = false
reason = "Develops bounded Passlib PBKDF2 and SHA-crypt field and record transforms without password hashing or verification; publication remains blocked until final acceptance."

[crates."base64-ng-pem"]
previous_version = "0.0.0"
version = "2.0.0"
change = "code"
publish = false
reason = "Develops the bounded RFC 7468 textual encoding companion; publication remains blocked until final acceptance."

[crates."base64-ng-serde"]
previous_version = "1.3.9"
version = "2.0.0"
change = "code"
publish = false
reason = "Tracks the synchronized 2.0 Serde integration; publication remains blocked until final acceptance."

[crates."base64-ng-bytes"]
previous_version = "1.3.9"
version = "2.0.0"
change = "code"
publish = false
reason = "Tracks the synchronized 2.0 bytes integration; publication remains blocked until final acceptance."

[crates."base64-ng-subtle"]
previous_version = "1.3.9"
version = "2.0.0"
change = "code"
publish = false
reason = "Tracks the synchronized 2.0 subtle integration; publication remains blocked until final acceptance."

[crates."base64-ng-tokio"]
previous_version = "1.3.9"
version = "2.0.0"
change = "code"
publish = false
reason = "Develops the synchronized 2.0 Tokio integration; publication remains blocked until final acceptance."

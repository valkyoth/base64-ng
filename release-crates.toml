[release]
version = "1.3.1"
policy = "synced-family"

[crates."base64-ng"]
previous_version = "1.3.0"
version = "1.3.1"
change = "code"
publish = true
reason = "Publishes the 1.3.1 family patch with updated async documentation and release evidence."

[crates."base64-ng-sanitization"]
previous_version = "1.3.0"
version = "1.3.1"
change = "dependency"
publish = true
reason = "Keeps the sanitization companion version synchronized with the 1.3.1 family release."

[crates."base64-ng-derive"]
previous_version = "1.3.0"
version = "1.3.1"
change = "dependency"
publish = true
reason = "Keeps the derive companion version synchronized with the 1.3.1 family release."

[crates."base64-ng-serde"]
previous_version = "1.3.0"
version = "1.3.1"
change = "dependency"
publish = true
reason = "Keeps the serde companion version synchronized with the 1.3.1 family release."

[crates."base64-ng-bytes"]
previous_version = "1.3.0"
version = "1.3.1"
change = "dependency"
publish = true
reason = "Keeps the bytes companion version synchronized with the 1.3.1 family release."

[crates."base64-ng-subtle"]
previous_version = "1.3.0"
version = "1.3.1"
change = "dependency"
publish = true
reason = "Keeps the subtle companion version synchronized with the 1.3.1 family release."

[crates."base64-ng-tokio"]
previous_version = "1.3.0"
version = "1.3.1"
change = "code"
publish = true
reason = "Publishes the admitted Tokio AsyncWrite streaming adapters, capacity-boundary tests, and async writer documentation."

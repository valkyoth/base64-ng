[release]
version = "1.0.9"
policy = "core-first-companion"

[crates."base64-ng"]
previous_version = "1.0.8"
version = "1.0.9"
change = "code"
publish = true
reason = "Adds the workspace publish plan and the companion-crate release boundary."

[crates."base64-ng-sanitization"]
previous_version = "0.0.0"
version = "1.0.9"
change = "code"
publish = true
reason = "Initial optional sanitization companion crate for CT decode into clear-on-drop secret containers."

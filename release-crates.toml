[release]
version = "1.3.0"
policy = "synced-family"

[crates."base64-ng"]
previous_version = "1.2.3"
version = "1.3.0"
change = "code"
publish = true
reason = "Publishes the 1.3.0 implementation-completion release with admitted strict SIMD decode dispatch, const decode helpers, final documentation, and release evidence."

[crates."base64-ng-sanitization"]
previous_version = "1.2.3"
version = "1.3.0"
change = "code"
publish = true
reason = "Syncs the optional sanitization integration with the 1.3.0 family and publishes locked-secret direct-fill guidance."

[crates."base64-ng-derive"]
previous_version = "1.2.3"
version = "1.3.0"
change = "code"
publish = true
reason = "Syncs derive companion package metadata and dependency examples with the 1.3.0 family release."

[crates."base64-ng-serde"]
previous_version = "1.2.3"
version = "1.3.0"
change = "code"
publish = true
reason = "Syncs serde companion package metadata and dependency examples with the 1.3.0 family release."

[crates."base64-ng-bytes"]
previous_version = "1.2.3"
version = "1.3.0"
change = "code"
publish = true
reason = "Syncs bytes companion package metadata and bounded helper documentation with the 1.3.0 family release."

[crates."base64-ng-subtle"]
previous_version = "1.2.3"
version = "1.3.0"
change = "code"
publish = true
reason = "Syncs subtle companion package metadata and comparison-boundary examples with the 1.3.0 family release."

[crates."base64-ng-tokio"]
previous_version = "1.2.3"
version = "1.3.0"
change = "code"
publish = true
reason = "Syncs Tokio companion package metadata, bounded helpers, and async reader streaming docs with the 1.3.0 family release."

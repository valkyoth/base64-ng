[release]
version = "2.0.0"
policy = "synced-family"

[crates."base64-ng"]
previous_version = "1.3.9"
version = "2.0.0"
change = "code"
publish = true
reason = "Publishes the reviewed 2.0 core API after the signed tag and final acceptance gate."

[crates."base64-ng-sanitization"]
previous_version = "1.3.9"
version = "2.0.0"
change = "code"
publish = true
reason = "Publishes the synchronized reviewed 2.0 sanitization integration."

[crates."base64-ng-derive"]
previous_version = "1.3.9"
version = "2.0.0"
change = "code"
publish = true
reason = "Publishes the synchronized reviewed 2.0 derive integration."

[crates."base64-ng-imap"]
previous_version = "0.0.0"
version = "2.0.0"
change = "code"
publish = true
reason = "Publishes the bounded reviewed RFC 3501 Section 5.1.3 modified-Base64 payload companion."

[crates."base64-ng-mime"]
previous_version = "0.0.0"
version = "2.0.0"
change = "code"
publish = true
reason = "Publishes the bounded reviewed RFC 2045 Section 6.8 content-transfer body companion."

[crates."base64-ng-multibase"]
previous_version = "0.0.0"
version = "2.0.0"
change = "code"
publish = true
reason = "Publishes the bounded reviewed Base64-family multibase companion against a pinned registry."

[crates."base64-ng-password"]
previous_version = "0.0.0"
version = "2.0.0"
change = "code"
publish = true
reason = "Publishes bounded reviewed Passlib PBKDF2 and SHA-crypt transforms without password hashing or verification."

[crates."base64-ng-openpgp"]
previous_version = "0.0.0"
version = "2.0.0"
change = "code"
publish = true
reason = "Publishes the bounded reviewed RFC 9580 OpenPGP ASCII armor companion."

[crates."base64-ng-pem"]
previous_version = "0.0.0"
version = "2.0.0"
change = "code"
publish = true
reason = "Publishes the bounded reviewed RFC 7468 textual encoding companion."

[crates."base64-ng-serde"]
previous_version = "1.3.9"
version = "2.0.0"
change = "code"
publish = true
reason = "Publishes the synchronized reviewed 2.0 Serde integration."

[crates."base64-ng-bytes"]
previous_version = "1.3.9"
version = "2.0.0"
change = "code"
publish = true
reason = "Publishes the synchronized reviewed 2.0 bytes integration."

[crates."base64-ng-subtle"]
previous_version = "1.3.9"
version = "2.0.0"
change = "code"
publish = true
reason = "Publishes the synchronized reviewed 2.0 subtle integration."

[crates."base64-ng-tokio"]
previous_version = "1.3.9"
version = "2.0.0"
change = "code"
publish = true
reason = "Publishes the synchronized reviewed 2.0 Tokio integration."

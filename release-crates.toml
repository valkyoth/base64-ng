[release]
version = "1.2.3"
policy = "synced-family"

[crates."base64-ng"]
previous_version = "1.2.2"
version = "1.2.3"
change = "code"
publish = true
reason = "Syncs the workspace crate family to 1.2.3 and publishes documentation for the sanitization dependency update."

[crates."base64-ng-sanitization"]
previous_version = "1.2.2"
version = "1.2.3"
change = "code"
publish = true
reason = "Updates the optional sanitization integration to sanitization 1.2.2 and syncs package metadata with the 1.2.3 family patch."

[crates."base64-ng-derive"]
previous_version = "1.2.2"
version = "1.2.3"
change = "code"
publish = true
reason = "Syncs companion crate package metadata and dependency examples with the 1.2.3 family patch."

[crates."base64-ng-serde"]
previous_version = "1.2.2"
version = "1.2.3"
change = "code"
publish = true
reason = "Syncs companion crate package metadata and dependency examples with the 1.2.3 family patch."

[crates."base64-ng-bytes"]
previous_version = "1.2.2"
version = "1.2.3"
change = "code"
publish = true
reason = "Syncs companion crate package metadata and dependency examples with the 1.2.3 family patch."

[crates."base64-ng-subtle"]
previous_version = "1.2.2"
version = "1.2.3"
change = "code"
publish = true
reason = "Syncs companion crate package metadata and dependency examples with the 1.2.3 family patch."

[crates."base64-ng-tokio"]
previous_version = "1.2.2"
version = "1.2.3"
change = "code"
publish = true
reason = "Syncs companion crate package metadata and dependency examples with the 1.2.3 family patch."
